import { HomeScreen } from 'compositions/screens/home'

/**
 * Связывает стартовый экран с корневым маршрутом.
 *
 * Используется для:
 *  - подключения стартового экрана к React Router
 */
export const HomeRoute = () => <HomeScreen />
