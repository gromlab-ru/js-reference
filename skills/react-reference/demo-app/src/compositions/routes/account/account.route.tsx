import { AccountScreen } from 'compositions/screens/account'

/**
 * Связывает экран кабинета с защищённым маршрутом.
 *
 * Используется для:
 *  - подключения кабинета к защищённой ветке React Router
 */
export const AccountRoute = () => <AccountScreen />
