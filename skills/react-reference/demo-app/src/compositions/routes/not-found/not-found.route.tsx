import { NotFoundScreen } from 'compositions/screens/not-found'

/**
 * Связывает экран неизвестного адреса с резервным маршрутом.
 *
 * Используется для:
 *  - подключения резервного экрана к React Router
 */
export const NotFoundRoute = () => <NotFoundScreen />
